<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Post</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

  <style>
    .edit-post-container {
      max-width: 600px;
      margin: 50px auto;
    }

    .image-preview {
      max-height: 300px;
      object-fit: cover;
      border-radius: 10px;
      margin-top: 15px;
    }
  </style>
</head>
<body class="bg-light">

  <div class="container edit-post-container">
    <h2 class="mb-4 text-center">Edit Your Post</h2>

    <form action="/posts/<%= post.id %>?_method=PATCH" method="POST" enctype="multipart/form-data" class="border p-4 shadow bg-white rounded">
      <div class="mb-3">
        <label class="form-label">Username</label>
        <input type="text" name="username" class="form-control" value="<%= post.username %>" disabled />
      </div>

      <div class="mb-3">
        <label class="form-label">Edit Caption</label>
        <textarea name="content" rows="3" class="form-control" required><%= post.content %></textarea>
      </div>

      <% if (post.imageUrl) { %>
        <div class="mb-3">
          <label class="form-label">Current Image</label><br />
          <img src="<%= post.imageUrl %>" class="image-preview" alt="Current Image" />
        </div>
      <% } %>

      <div class="mb-3">
        <label class="form-label">Change Image (optional)</label>
        <input type="file" name="image" accept="image/*" class="form-control" />
      </div>

      <button type="submit" class="btn btn-primary w-100">Update Post</button>
    </form>

    <div class="text-center mt-3">
      <a href="/posts" class="btn btn-link">← Back to All Posts</a>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
