<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Post Detail</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

  <style>
    .post-card {
      max-width: 600px;
      margin: 50px auto;
    }

    .post-image {
      width: 100%;
      max-height: 400px;
      object-fit: cover;
      border-radius: 10px;
    }
  </style>
</head>
<body class="bg-light">

  <div class="container post-card">
    <div class="card shadow p-3">
      <% if (post.imageUrl) { %>
        <img src="<%= post.imageUrl %>" class="post-image mb-3" alt="Post Image" />
      <% } %>

      <div class="card-body">
        <h6 class="text-muted">@<%= post.username %></h6>
        <p class="card-text"><%= post.content %></p>

        <div class="d-flex justify-content-between mt-3">
          <a href="/posts/<%= post.id %>/edit" class="btn btn-outline-secondary btn-sm">Edit</a>

          <form action="/posts/<%= post.id %>?_method=DELETE" method="POST">
            <button type="submit" class="btn btn-danger btn-sm">Delete</button>
          </form>
        </div>

        <div class="mt-3">
          <a href="/posts" class="btn btn-link">← Back to all posts</a>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
